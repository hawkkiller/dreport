name: dreport
description: Parse and convert Dart analysis, test reports to other formats.
version: 0.0.4
repository: https://github.com/hawkkiller/dreport

executables:
  dreport: dreport

environment:
  sdk: ^3.0.0

dependencies:
  args: ^2.5.0
  cli_util: ^0.4.2
  crypto: ^3.0.6

dev_dependencies:
  test: ^1.24.0
  sizzle_lints: ^2.1.7
